const BoardManagerPopupWindowEditerFormHook = ({
	boardName: rawBoardName,
	columns: rawColumns,
}: BoardManagerPopupWindowFormData): void => {
	console.log(rawBoardName)
	console.log(rawColumns)
}

export default BoardManagerPopupWindowEditerFormHook
