type Popup = {
	active: boolean
	content: React.ComponentType<any> | undefined
}

type Active = {
	active: boolean
}

type UserInputTitle = {
	title: string
}

type UserInput = {
	title?: string
	placeHolder?: string
}
